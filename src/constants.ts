export const MAIN_PATH = "/";
export const NEW_PATH = "/new";
export const EDIT_ID_PATH = (id: string) => `/edit/${id}`;

export const PAGE_SIZE = 5;

export const UNDO_TOAST_TIMEOUT_MS = 10000; // 10s
